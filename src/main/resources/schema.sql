CREATE TABLE cats (
                         id   LONG      NOT NULL AUTO_INCREMENT,
                         name VARCHAR(128) NOT NULL,
                         age VARCHAR(20),
                         PRIMARY KEY (id)
);